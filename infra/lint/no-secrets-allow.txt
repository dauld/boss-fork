# no-secrets-allow.txt — allow-list for infra/lint/no-secrets.sh.
#
# Format: one entry per line, `path:pattern-id`. The path side may be
# a glob (matched against the repo-relative path). `#` starts a
# comment, full-line or trailing. Every entry MUST carry a comment
# explaining why the hit is NOT a secret — the lint prints every
# suppression it performs on every run, so this file is visible
# policy, not a silencer. An entry without a why does not merge.
#
# pattern-ids: private-key kubeconfig-data talos-cert-bundle
#              wireguard-key token-assignment url-token gcp-sa-json
#
# No entries yet: the 2026-08-12 triage run over all 1385 tracked
# files produced zero hits on all seven patterns — even the looser
# sweeps (16+-char values, any-payload markers, common vendor token
# prefixes) found only code identifiers and the known-public local
# dev password `boss`. The first real fixture that trips the lint
# lands here, with its why.
